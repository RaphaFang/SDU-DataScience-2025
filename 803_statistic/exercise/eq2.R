# 丟機率（比例）→ 回 Z（位置）
qnorm(0.975) # 1.959964

# 這是問區間內有多少。丟 Z（位置）→ 回機率（比例）
pnorm(1.45) - pnorm(-1.45)  # 0.8529415

qnorm(0.34)

pnorm(1.9) - pnorm(-1.4)
# 0.8905268

sqrt(3^2 + 6^2 + 1^2 + 11^2 + 2^2 )
# 13.0767

pnorm(100, 81, 13.1) - pnorm(80, 81, 13.1)
# 這就會是計算 80~100之間的機率

left = qnorm(0.1, 81, 13.1)
right = qnorm(0.9, 81, 13.1)
cat(left, right)
