# direct proof template
# 給出假設，

# ---------------------------------------------------------------
# statement: k's even, k square must be even.
# by definition, even 意味著可以被 2 整除
# 可以理解成 k 的原先構成可以理解成，2*i
# 所以 k square，會是 (2*i) * (2*i)
# QED. 至少會有兩個 2 在裡面，所以必然是 even

# ---------------------------------------------------------------
# a.1.
# statement: 若整數 n 為 3 的倍數，則 n² 亦為 3 的倍數。
# 如同上面作法

# a.2. 
# statement: 如果 status='SHIPPED'，那麼 status 不等於 'CANCELLED'
# status 欄位值唯一且互斥
# QED. 如果不能並存，如果已經標記為一者，則不可能為是另一者


# ---------------------------------------------------------------
# a.3.
# 證：對任何整數 n，n² ≡ 0 或 1 (mod 4)。
    # ≡	同餘（congruence）	「在指定的模數下有相同餘數」的意思。例如 a ≡ b 表示 a 與 b 的差可以被模數整除。
    # (mod 4)	指定模數 4	「在模 4 的世界裡」或「以 4 為基準做餘數比較」。寫在最後是整句的條件，讀作「mod four」。
        # 要先除上 mod，接著檢查 ≡ 左右於數是否相同
# even 會是 2k -> 4k square
# odd 會是 2k+1 -> 4k square + 4k + 1
# 除上 4 ，於數不是0就是1，QED
 
# a.4.
# 欄位 amount 可能為負、0、正。statement: 如果 amount≠0，則 amount/|amount| 的結果為±1
# 排除為0的情況，並且分母必定是正數，
# 這時分子amount可能正可能負，除上為正數的amount，則可得到的結果必然是±1

# ---------------------------------------------------------------
# a.5.
# 整除-同餘混合
# 證：若 4 | n ⇒ n² ≡ 0 (mod 16)。
    # 4 | n，可以理解成 n = 4*k
    # (4*k)*(4*k) = 16*k square ，可以被16整除
    # QED. n² ≡ 0 因為都能被16整除

# 整數不等式
# 證：對 n ≥ 1，n² + n ≥ 2n
    # n*(n +1), n*(2)
    # QED. n +1 ≥ 2 

# 不存在型
# 證：不存在整數 n 使 n² = 2n + 1
    # n² -2n - 1 = 0
    # n=1 ± 跟號2
    # 無論哪一個答案都不是整數，所以不存在

# 資料欄位範例
# 欄位 score 已知置於區間 [0, 1]。證：若 score > 0.8 ⇒ score² > 0.64
    # 如果 score > 0.8，那麼他的次方就是score²，而不等是右邊同樣次方得到0.64
    # 得到score² > 0.64


